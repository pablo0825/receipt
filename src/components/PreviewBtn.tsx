const PreviewBtn = () => {
  return (
    <button className="py-1 px-3 bg-amber-300 rounded-full text-white hover:bg-amber-400 transition transform hover:scale-105 active:scale-95">
      預覽
    </button>
  );
};

export default PreviewBtn;
